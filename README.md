# yomama

Startup landing page built with Next.js for a CLI tool that helps you "rent your mama" from the terminal.

This is a testing repo for doing playing around with Codex, Claude Code, and OpenClaw.

## Run locally

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`.
